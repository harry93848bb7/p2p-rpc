syntax = "proto3";
package protocol;
option go_package = "./protocol";

message Ping {
    string message = 1;
    int64 timestamp = 2;
}

message Pong {
    string message = 1;
    int64 timestamp = 2;
}